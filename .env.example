# Example environment variables for MCP server
# Rename to .env and set values as needed

# Provider: 'mock' or 'openai'
MCP_PROVIDER=mock
MCP_MODEL=mock-model

# If using OpenAI provider, set your API key here (DO NOT commit real keys)
# OPENAI_API_KEY=sk-...
